# Environment Variables

# ========== DATABASE ==========
# MongoDB connection string
# Local MongoDB: mongodb://localhost:27017/hercycle
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/hercycle
MONGODB_URI=mongodb://localhost:27017/hercycle

# ========== AUTHENTICATION ==========
# JWT secret for token signing (generate a strong random string)
# Generatesecure key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# ========== EMAIL CONFIGURATION ==========
# Gmail account for sending emails (doctor approvals, warnings, password resets)
EMAIL_USER=hercyle806@gmail.com
# Gmail App Password (NOT your regular Gmail password!)
# Generate at: https://myaccount.google.com/apppasswords
EMAIL_PASS=your-gmail-app-password-here

# ========== STRIPE PAYMENT ==========
# Stripe secret key for processing payments
# Test key: sk_test_...
# Live key: sk_live_...
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Stripe publishable key (used in client-side, safe to expose)
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# ========== SERVER CONFIGURATION ==========
# Port for Express server
PORT=5000

# Node environment (development | production)
NODE_ENV=development

# Client URL for CORS (adjust for production)
CLIENT_URL=http://localhost:5173

# ========== OPTIONAL ==========
# Session secret for express-session (if using sessions)
# SESSION_SECRET=your-session-secret

# API rate limiting
# MAX_REQUESTS_PER_MINUTE=100
